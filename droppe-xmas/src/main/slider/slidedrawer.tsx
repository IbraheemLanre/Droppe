/*import React, { ReactNode } from "react";

export function SlideDrawer({
  drawerClasses,
}: {
  drawerClasses: (title: string) => ReactNode;
}) {
  const [isOpen, setIsOpen] = React.useState<string>("side-drawer");
  return (
    <div>
      <h2>{drawerClasses(isOpen)}</h2>
      <div>
        <button onClick={() => setIsOpen("side-drawer open")}></button>
      </div>
    </div>
  );
}*/
